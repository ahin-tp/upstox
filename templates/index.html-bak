<!DOCTYPE html>
<html>
<head>
    <title>Upstox 9:15 SL Bot</title>
    <style>
        body { font-family: Arial, sans-serif; }
        table { border-collapse: collapse; width: 100%; }
        th, td { padding: 8px; text-align: center; border: 1px solid #ccc; }

        .status-PENDING {
            background-color: #fff3cd;
            color: #856404;
            font-weight: bold;
        }

        .status-EXECUTED {
            background-color: #d4edda;
            color: #155724;
            font-weight: bold;
        }

        .status-CANCELLED {
            background-color: #f8d7da;
            color: #721c24;
            font-weight: bold;
        }

        .status-EXITED {
            background-color: #d1ecf1;
            color: #0c5460;
            font-weight: bold;
        }

        .btn-disabled {
            color: gray;
            pointer-events: none;
            text-decoration: none;
        }

        .btn {
            padding: 4px 8px;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
        }

        .btn-cancel { color: #dc3545; }
        .btn-exit { color: #007bff; }
    </style>
</head>

<body>

<h2>Upstox 9:15 SL Bot</h2>

<form method="post">
    <label>Instrument:</label><br>
    <input id="instrument-search" placeholder="Type RELIANCE / INFY" autocomplete="off">
    <input name="instrument" id="instrument-token" required readonly>
    <div id="results" style="border:1px solid #ccc; display:none;"></div>
    <br><br>

    <label>Instrument Token:</label><br>
    <input name="instrument" required><br><br>

    <label>Quantity:</label><br>
    <input name="qty" type="number" required><br><br>

    <label>Trigger Price:</label><br>
    <input name="trigger" type="number" step="0.05" required><br><br>

    <label>Limit Price:</label><br>
    <input name="limit" type="number" step="0.05" required><br><br>

    <label>Stop Loss Price:</label><br>
    <input name="sl" type="number" step="0.05" required><br><br>

    <button type="submit">Add Order</button>
</form>

<hr>

<h3>Orders</h3>

<table>
    <tr>
        <th>ID</th>
        <th>Instrument</th>
        <th>Qty</th>
        <th>Trigger</th>
        <th>Limit</th>
        <th>SL</th>
        <th>Status</th>
        <th>Actions</th>
    </tr>

    {% for o in orders %}
    <tr>
        <td>{{ o[0] }}</td>
        <td>{{ o[1] }}</td>
        <td>{{ o[2] }}</td>
        <td>{{ o[3] }}</td>
        <td>{{ o[4] }}</td>
        <td>{{ o[5] }}</td>

        <!-- STATUS -->
        <td class="status-{{ o[8] }}">
            {{ o[8] }}
        </td>

        <!-- ACTIONS -->
        <td>
            <!-- CANCEL -->
            {% if o[8] == "PENDING" %}
                <a class="btn btn-cancel"
                   href="/cancel/{{ o[0] }}"
                   onclick="return confirm('Cancel this order?');">
                   ❌ Cancel
                </a>
            {% else %}
                <span class="btn btn-disabled">❌ Cancel</span>
            {% endif %}

            |

            <!-- EXIT -->
            {% if o[8] == "EXECUTED" %}
                <a class="btn btn-exit"
                   href="/exit/{{ o[0] }}"
                   onclick="return confirm('Exit at MARKET price?');">
                   🚪 Exit
                </a>
            {% else %}
                <span class="btn btn-disabled">🚪 Exit</span>
            {% endif %}
        </td>
    </tr>
    {% endfor %}
</table>

</body>
</html>
